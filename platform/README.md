Some headers in `/include` need platform specific implementation.
This folder contains implementation of those headers for different platforms.
`posix` folder is included for both macOS and Linux builds.