#include "Pathfinder.h"

int PathfinderMapFromLevel(PathfinderMap* pm, Level* level) {
	
	return 0;
}

// iterate over objects, test (or find out which grid locations it occupies)
// put them in a integer map
