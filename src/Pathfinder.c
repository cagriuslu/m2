#include "Pathfinder.h"

int PathfinderMapFromLevel(PathfinderMap* pm, Level* level) {
	// TODO
	// Iterate over object
	// Check their category using body->fixture->filter->category
	// Iterate over shapes and combine AABBs
	// Mark all grids that collide with AABB
	// Put marked ones in a map
	return 0;
}
